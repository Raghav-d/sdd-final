You are performing a read-only codebase archaeology session.
Do not modify any files except creating specs/architecture.md.

Read in this order:

1. package.json — confirm React, Vite, Redux Toolkit, React Query,
   Omni versions, LightFrame config block
2. find . -name "*.tsx" -not -path "*/node_modules/*"
3. find . -name "*.slice.ts" -not -path "*/node_modules/*"
4. find . -name "*.query.ts" -not -path "*/node_modules/*"
5. find . -name "*.api.ts" -not -path "*/node_modules/*"
6. find . -name "*.hooks.ts" -not -path "*/node_modules/*"
7. src/store/ or wherever Redux store is configured

Also read: ../HORIZON-pme-calibration-api/specs/openapi.yaml
if accessible, to cross-reference API calls made in the UI.

Produce specs/architecture.md with exactly these sections:

## Stack
Confirmed versions. LightFrame packageType and registered routes.
Omni component version in use.

## Component tree
Every component — name, responsibility in one line,
what data it reads, what it dispatches or mutates.

## Redux state shape
Every slice — name, state fields, key actions.

## React Query usage
Every useQuery/useMutation — key, endpoint called,
which component owns it.

## API calls
Table format:
| Component/Hook | Method | Endpoint | Auth header |
|----------------|--------|----------|-------------|

## Omni components used
List every Omni component imported across the codebase.
Note any non-standard usage or overrides.

## LightFrame config
packageType, any registered routes or plugins,
how the container mounts.

## Patterns observed
Naming conventions, error handling, loading state patterns,
how SSO token is attached to requests.

## Decisions log
(Leave empty — append-only, never generated)

When done print: UI archaeology complete


find . -name "*.plugin.ts" -not -path "*/node_modules/*"
find . -name "*.ts" -path "*/routes/*" -not -path "*/node_modules/*"

cd HORIZON-pme-calibration-api

find . -name "*.ts" -not -path "*/node_modules/*" | head -40

grep -r "fastify.register\|app.register\|router.get\|router.post\|addHook\|RouteOptions" src/ --include="*.ts" -l

find . -name "*.ts" -path "*/routes/*" -not -path "*/node_modules/*"
find . -name "*.ts" -path "*/controllers/*" -not -path "*/node_modules/*"
find . -name "*.ts" -path "*/models/*" -not -path "*/node_modules/*"
find . -name "*.ts" -path "*/schemas/*" -not -path "*/node_modules/*"

Read-only codebase archaeology. Create specs/architecture.md and
specs/openapi.yaml only. Do not modify any other files.

Read in this order:

1. package.json
2. src/routes/index.ts
3. cat src/routes/health/index.ts
4. cat src/routes/peerComparison/index.ts
5. cat src/routes/liveCalibrationSession/index.ts
6. cat src/routes/calibrationGroup/index.ts
7. cat src/routes/calibrationSession/index.ts
8. cat src/routes/calibrationTemplate/index.ts
9. cat src/routes/organizationalHealth/index.ts
10. cat src/routes/leaders/index.ts
11. cat src/routes/task/index.ts
12. cat src/routes/workerRatings/index.ts
13. cat src/routes/v2/calibrationGroup/index.ts
14. cat src/routes/v2/calibrationTemplate/index.ts
15. find . -name "*.ts" -path "*/database/models/*" -not -path "*/node_modules/*" -not -name "*.test.ts"
16. Read each model file found above
17. find . -name "*.ts" -path "*/services/*" -not -path "*/node_modules/*" -not -name "*.test.ts"
18. Read each service file found above

Produce specs/architecture.md with exactly these sections:

## Stack
Confirmed package versions: Fastify, Inversify, Sequelize,
TypeBox/Zod, Node.js. Note any Capital One specific packages.

## Routes
| Method | Path | Handler location | Auth required |
Request schema | Response schema |
|--------|------|-----------------|---------------|
|--------|------|-----------------|---------------|
Include v2 routes separately, note what differs from v1.

## Sequelize models
Each model: table name, fields + types, associations,
any hooks defined.

## Service layer
Each service file: responsibility, methods, what model it touches.

## Validation schemas
Every TypeBox/Zod schema found — name, shape, which route uses it.

## SSO / Auth pattern
How authentication is applied — middleware, hooks, decorators.
Which routes are protected and which are open (e.g. health).

## Error handling pattern
How errors are caught and returned — shape of error responses.

## Patterns observed
Naming conventions, folder structure logic, anything
a developer needs to know before modifying this codebase.

## Decisions log
(Leave empty — append-only, never generated)

Then produce specs/openapi.yaml derived from the actual route
handlers and schemas. Reflect what the code does, not assumptions.
One endpoint per route method found. Include request body schemas,
response schemas for 200/201, 400, 401, 404, 500.

When done print: API archaeology complete

Read-only codebase archaeology. Create specs/architecture.md only.
Do not modify any other files.

Read in this order:

1. package.json
2. find . -name "*.tsx" -path "*/features/*" -not -path "*/node_modules/*" -not -name "*.test.tsx"
3. Read each component file found above
4. find . -name "*.slice.ts" -not -path "*/node_modules/*"
5. Read each slice file found above
6. find . -name "*.query.ts" -not -path "*/node_modules/*"
7. find . -name "*.api.ts" -not -path "*/node_modules/*"
8. find . -name "*.hooks.ts" -not -path "*/node_modules/*"
9. Read all files found in steps 6-8
10. find . -name "store.ts" -o -name "store/index.ts" -not -path "*/node_modules/*"
11. Read store file found above
12. cat ../HORIZON-pme-calibration-api/specs/openapi.yaml (if accessible)

Produce specs/architecture.md with exactly these sections:

## Stack
Confirmed versions: React, TypeScript, Vite, Omni version,
Redux Toolkit, React Query, LightFrame packageType and config.
Yarn workspaces structure if present.

## Component tree
Every component found — name, responsibility in one line,
what data it reads, what it dispatches or mutates.
Format:
| Component | Responsibility | Data source | Actions |
|-----------|---------------|-------------|---------|

## Redux state shape
Every slice — name, state fields, key actions, async thunks.

## React Query usage
Every useQuery/useMutation:
| Hook name | Query key | Endpoint called | Owner component |
|-----------|-----------|-----------------|-----------------|

## API calls
Every API call made from the UI:
| Component/Hook | Method | Endpoint | Auth header attached |
|----------------|--------|----------|---------------------|

## Omni components used
Every Omni component imported across the codebase.
Note any custom overrides or non-standard usage.

## LightFrame config
packageType, registered routes, how container mounts,
any plugins registered.

## Patterns observed
Naming conventions actually used (not what docs say).
Error handling pattern.
Loading state pattern.
How SSO token is attached to API requests.
Any custom hooks that wrap Omni or Redux.

## Gaps observed
Anything missing — untested components, inconsistent patterns,
API calls without error handling, components without loading states.

## Decisions log
(Leave empty — append-only, never generated)

When done print: UI archaeology complete


manually create specs/prd.md

# PRD — [Ticket title]
**Jira:** [ticket ID]

## Summary
[Copy ticket description]

## Acceptance Criteria
- AC-1: [first AC]
- AC-2: [second AC]
...

## Affected files
[Your best guess at which files need changing —
 archaeology.md will make this more precise]

## Out of scope
[Anything the ticket explicitly excludes]

# session 3 prompt
Read these files first before doing anything:
- specs/prd.md
- specs/architecture.md
- specs/openapi.yaml (from API repo if accessible)

Then run:
/sdd-modify [path to affected component]

If you cannot determine the affected file from architecture.md,
list the candidates and ask me to choose before proceeding.
Do not guess. Do not modify any file until I confirm the target.

--
Read specs/prd.md and specs/architecture.md.
Then use sdd-generate to create the new acknowledgment feature
following the patterns observed in architecture.md exactly.

--Tests
grep -n "freezeTableName\|UUID\|cretd_ts\|ownr_eid" \
  src/database/models/chatSession/index.ts

 grep -n "transaction\|Transaction" \
   src/services/acknowledgment/index.ts

grep -n "auth\|preHandler" src/routes/acknowledgment/index.ts

grep -n "describe\|it(" \
  src/routes/acknowledgment/index.test.ts
--- new prompt for modify
Read src/routes/acknowledgment/index.ts and
src/routes/calibrationTemplate/index.ts.

The acknowledgment route is missing SSO authentication.
Looking at calibrationTemplate/index.ts as the reference pattern,
add the correct auth property to the RouteOptions object in
src/routes/acknowledgment/index.ts so it follows the same
SSO pattern as all other authenticated routes.

Do not change anything else in the file.
After editing, confirm the auth property is present.

--New prompt for architecture.md
Read-only codebase archaeology. Create or refresh specs/architecture.md
and specs/openapi.yaml only. Do not modify any other files.

STEP 0 — Check if files already exist:
!`ls specs/architecture.md 2>/dev/null && echo "EXISTS" || echo "NEW"`

If EXISTS:
- Read specs/architecture.md first
- Extract and preserve the ## Decisions log section exactly as-is
- Note any lines starting with "NOTE:" or "> " — preserve these too
- Print: "Existing architecture.md found — Decisions log will be preserved"
- Then proceed to regenerate all other sections from the codebase

If NEW:
- Print: "No existing architecture.md found — generating from scratch"
- Proceed directly to STEP 1

STEP 1 — Discover full source structure:
!`find src -maxdepth 3 -type d | sort`

Examine every folder found. For each folder determine its purpose.
If a folder's purpose is unclear, read one file from it before
proceeding. Do not skip any folder.

STEP 2 — Read package.json:
!`cat package.json`

STEP 3 — Read all route files:
!`find src/routes -name "index.ts" -not -name "*.test.ts"`
Read every file returned.

STEP 4 — Read all database files (models, DAOs, migrations):
!`find src/database -name "*.ts" -not -path "*/node_modules/*" -not -name "*.test.ts"`
Read every file returned.

STEP 5 — Read all service files:
!`find src/services -name "*.ts" -not -path "*/node_modules/*" -not -name "*.test.ts"`
Read every file returned.

STEP 6 — Read all remaining folders discovered in STEP 1
that have not yet been read:
For each remaining folder run:
!`find src/<folder> -name "*.ts" -not -path "*/node_modules/*" -not -name "*.test.ts"`
Read every file returned.
This ensures no folder is missed regardless of naming convention.

STEP 7 — Read server entry point:
!`cat src/server.ts 2>/dev/null || cat src/app.ts 2>/dev/null`
This reveals the DI wiring and how all pieces connect.

Now produce specs/architecture.md with exactly these sections:

---

## Last refreshed
[Today's date and time]

## Source tree
[Paste exact output of: find src -maxdepth 3 -type d | sort]

## Stack
Confirmed package versions from package.json:
- Runtime, framework, ORM, validation library
- All Capital One specific packages with their purpose

## Routes
| Method | Path | Handler location | Auth required | Request schema | Response schema |
|--------|------|-----------------|:---:|---------------|-----------------|
List all v1 routes first, then v2 routes in a separate table.
Note what v2 routes differ from v1.

## Sequelize models
For each model:
- Table name (exact, as defined in freezeTableName)
- All fields: JS name | DB column | Type | Nullable | Default
- Associations defined
- Hooks defined

## DAOs
For each DAO file found:
- File location
- Responsibility in one line
- Methods: name, what it does, which model or raw SQL it uses
- Which service(s) consume it

## Service layer
For each service:
- File location
- Responsibility in one line
- Methods: name, what it does
- Which DAO(s) it calls
- Which other services it depends on

## Validation schemas
For each Zod schema found:
- Name
- Shape (fields and types)
- Which route or service uses it

## SSO / Auth pattern
- How auth is applied globally (which function, which file)
- Which routes are open (auth: false)
- How permissions are checked per route
- What request.user contains after auth

## Error handling pattern
- Global error handler location and shape
- Per-handler try/catch pattern
- Error classes and their HTTP status codes
- Error response shape

## Patterns observed
Naming conventions actually used in this codebase:
- Controllers, Services, DAOs, Models, Schemas
- File and folder naming
- Import conventions (ESM .js extensions etc)
- Any patterns a new developer must know before modifying code

## Decisions log
(Append-only — never overwritten by archaeology runs)
[If EXISTS: reinsert preserved content here exactly]
[If NEW: leave empty]

---

Then produce specs/openapi.yaml:
- Derived from actual route handlers and Zod schemas
- One endpoint per route method found
- Request body schemas matching actual Zod validation
- Response schemas for 200/201, 400, 401, 404, 500
- Always regenerated from scratch — no preservation needed

When done print exactly: API archaeology complete

-- UI Prompt
Read-only codebase archaeology. Create specs/architecture.md only.
Do not modify any other files. Do not create openapi.yaml —
that comes from the API repo.

STEP 0 — Check if file already exists:
!`ls specs/architecture.md 2>/dev/null && echo "EXISTS" || echo "NEW"`

If EXISTS:
- Read specs/architecture.md first
- Preserve ## Decisions log exactly as-is
- Print: "Existing architecture.md found — Decisions log preserved"

If NEW:
- Print: "No existing architecture.md — generating from scratch"

STEP 1 — Discover full source structure:
!`find src -maxdepth 3 -type d | sort`

Examine every folder. If a folder's purpose is unclear,
read one file from it before proceeding. Do not skip any folder.

STEP 2 — Read package.json:
!`cat package.json`

Also read if present:
!`cat yarn.lock 2>/dev/null | head -5 || echo "NO_YARN"`
!`cat vite.config.ts 2>/dev/null || cat vite.config.js 2>/dev/null || echo "NO_VITE"`

STEP 3 — Read LightFrame config:
!`cat lightframe.config.ts 2>/dev/null || cat lightframe.config.js 2>/dev/null || echo "NO_LIGHTFRAME_CONFIG"`

Read the lightframe packageType from package.json carefully.

STEP 4 — Read all feature/component files:
!`find src -name "*.tsx" -not -path "*/node_modules/*" -not -name "*.test.tsx" -not -name "*.stories.tsx"`
Read every file returned.

STEP 5 — Read all state management files:
!`find src -name "*.slice.ts" -not -path "*/node_modules/*"`
!`find src -name "*.store.ts" -not -path "*/node_modules/*"`
!`find src -name "store.ts" -not -path "*/node_modules/*"`
!`find src -name "store" -type d -not -path "*/node_modules/*"`
Read every file returned.

STEP 6 — Read all API/query files:
!`find src -name "*.query.ts" -not -path "*/node_modules/*"`
!`find src -name "*.api.ts" -not -path "*/node_modules/*"`
!`find src -name "*.service.ts" -not -path "*/node_modules/*"`
!`find src -name "*.hooks.ts" -not -path "*/node_modules/*"`
Read every file returned.

STEP 7 — Read remaining folders from STEP 1 not yet covered:
For each folder not yet read, run:
!`find src/<folder> -name "*.ts" -not -path "*/node_modules/*" -not -name "*.test.ts"`
Read every file returned.

STEP 8 — Cross-reference API contract if accessible:
!`cat ../HORIZON-pme-calibration-api/specs/openapi.yaml 2>/dev/null | head -100 || echo "NO_API_SPECS"`

If found, note which endpoints the UI currently calls vs what
the API exposes — flag any gaps.

Now produce specs/architecture.md with exactly these sections:

## Last refreshed
[Today's date and time]

## Source tree
[Paste exact output of: find src -maxdepth 3 -type d | sort]

## Stack
- React version, TypeScript version
- Vite version and config summary
- Omni/Gravity design system version
- Redux Toolkit version (if present)
- React Query version (if present)
- LightFrame packageType and version
- Yarn workspaces structure if present
- All Capital One specific packages with purpose

## LightFrame config
- packageType value
- Registered routes (if any)
- Plugin registrations
- How the container mounts into the shell

## Component tree
| Component | File path | Responsibility | Data source | Actions dispatched |
|-----------|-----------|---------------|-------------|-------------------|
One row per component found.

## Redux state shape
For each slice:
- Slice name and file path
- State fields and types
- Key actions and thunks
- What component(s) consume it

## React Query usage
| Hook name | Query key | Endpoint called | Method | Owner component |
|-----------|-----------|-----------------|--------|-----------------|

## API calls
Every API call made from the UI:
| Component/Hook | Method | Endpoint | Auth header attached | Error handled |
|----------------|--------|----------|---------------------|---------------|

## Omni/Gravity components used
Every design system component imported across the codebase.
Note any custom overrides, theme tokens used, or non-standard usage.

## Patterns observed
- Naming conventions actually used (files, components, hooks, slices)
- Error handling pattern (how errors surface to the user)
- Loading state pattern (skeleton, spinner, suspense?)
- How SSO token is attached to API requests
- Any custom hooks that wrap Omni or Redux
- Routing approach (React Router, LightFrame routing, or both)

## Gaps observed
- Components without error handling
- API calls without loading states
- Endpoints in openapi.yaml not called by the UI
- UI calls to endpoints not in openapi.yaml

## Decisions log
(Append-only — never overwritten by archaeology runs)
[If EXISTS: reinsert preserved content here exactly]
[If NEW: leave empty]

When done print: UI archaeology complete

--figma check
python3 ~/HORIZON-aide-toolkit/skills/figma/export_file.py \
  --url "https://www.figma.com/design/5wU5CNTExhZBnBQee1P3Tn/..." \
  --out specs/figma-export.json